export const PreferencesNames = {
  REPO_SEARCH_DIR: "pref.searchDir",

  DEFAULT_DEITOR: "pref.defaultEditor",

  CLASH_CONTROLLER_HOST: "pref.clash.host",
  CLASH_CONTROLLER_PORT: "pref.clash.port",
  CLASH_CONTROLLER_ENABLED: "pref.clash.enabled",
}

export const StorageNames = {
  REPOS: "store.repos",
}

